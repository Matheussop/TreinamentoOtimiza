Neste documentos estão apresentados alguns comandos básico da linguagem c#

Comandos:
    1° comando:
        Console.WriteLine(); // Usado para escrever na tela um texto ou o valor de uma váriavel, após a escrita ira pular uma linha
    2° comando:
        Console.Readkey(); // Usado para ler do teclado
    3° comando:
        comando importante "foreach" 
        Exemplo:
            string nomes[] = new string [10];
            foreach (string nome in nomes){
                 // a variavel "nome" recebe os valores da array "nomes"
            }
    4° comando:
        Crição de lista
        Exemplo:
        List<string> nomeLista = new List<string>();
    5° comando:
        override //sobre escrita de metodos.
    6° comando: 
        comandos para comentarios usando /// <sumary> indicara oq aquela variavel o metodo faz.
                                         /// <param name = "medida">  comentar a respeito de um parametro no caso aqui o parametro medida.
        é possivel criar o comando #region para registrar uma parte do codigo em um bloco 
        Exemplo:
            #region Matheus Luiz Oliveira
            dicionario.Add("T1",128);
            dicionario.Add("T2",256);
            Console.WriteLine(dicionario["T1"]);
            Console.ReadKey();
            #endregion
        
        comentarios para tarefas a serem realizadas //TODO , e o texto a respeito da tarefa a ser feita
    7° comando:
        comandos relacionados a malipulação de textos 
            Texto.IndexOd(palavra) // localiza a primeira palavra que é igual a palavra escolhida no texto
            Texto.Replace("palavraA","palavraB") // retorna um texto com a palavraA substituida pela palavraB.
            Texto.StartWith("Ola") // verifica se o texto inicia pela palavra Ola
            Texto.EndsWith("mundo") // verifica se o texto tem no fim a palavra mundo
            Texto.Contains("world") // verifica se o texto possuoi a palavra world
            String.Format // permite formatar uma string com variaveis
            os comandos toUpper e ToLower aumenta e diminui respectivamente as letras dos textos
            Exemplo:
                texte.toUpper(); 
            O comando Trim (TrimStart(),TrimEnd()) remove os espaços de um texto .
            O comando Split separa um texto por um simbolo ou texto escolhido , retornando um array com os textos separados
            Exemplo: 
                string[] nomesArray = nomes.Split(','); no caso separar por virgula.
            texto.Substring(13,6 /*ou apenas 13*/); retirar parte de um texto ou de uma posição em diante o de uma posicao definida